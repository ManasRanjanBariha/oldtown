import { createTheme } from "@mui/material";

export const Theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "skyblue",
      black: "black",
    },
    secondary: {
      main: "#000",
    },
    otherColor: {
      main: "#999",
    },
    white:{
      main:"#fff"
    }
  },
});
